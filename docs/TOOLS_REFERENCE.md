# Atomik Araç Referansı 

Bu belge, Atomik'in optimize edilmiş yeni araç setini açıklar. ~65 araçlık eski sistem, ~30 güçlü ve birleşik araca dönüştürülmüştür.

---

## 🧠 Birleşik Hafıza Araçları (Unified Memory)

Eski `save_context`, `remember_this`, `log_mood`, `learn_from_feedback` vb. yerine tek bir giriş noktası.

### 1. `manage_memory`
Bilgi kaydetmek, güncellemek veya silmek için kullanılır.

- **Kullanım:** `manage_memory(action, category, key, content)`
- **Kategoriler:**
  - `context`: Kısa vadeli anahtar-değer (örn. sınav tarihi)
  - `long_term`: Uzun vadeli RAG hafızası (örn. sohbet özeti)
  - `visual`: Görsel gözlemler (örn. kullanıcının giyimi)
  - `mood`: Ruh hali takibi
  - `project`: Proje durumu takibi
  - `learning`: Öğrenilen kurallar
  - `proactive_rule`: Proaktif davranış kuralları

**Örnekler:**
```python
# Sözel bilgi kaydet
manage_memory(action="save", category="context", key="sınav", content="28 Aralık'ta matematik sınavı var")

# Görsel gözlem kaydet
manage_memory(action="save", category="visual", content="Kullanıcı bugün yorgun görünüyor")

# Önemli not al (RAG)
manage_memory(action="save", category="long_term", key="toplantı", content="Proje teslim tarihi 5 Ocak'a çekildi")
```

### 2. `query_memory`
Her türlü bilgiyi sorgulamak için kullanılır.

- **Kullanım:** `query_memory(query, filter_type, time_range)`
- **Filtreler:** `all`, `context`, `long_term`, `chat` (sohbet geçmişi), `visual`, `learning`

**Örnekler:**
```python
# Genel arama
query_memory(query="sınav")

# Görsel geçmiş sorgusu
query_memory(query="nasıl görünüyorum?", filter_type="visual")

# Sohbet geçmişi (son 1 gün)
query_memory(query="dün ne konuştuk?", filter_type="chat", time_range=1)
```

---

## 👁️ Birleşik Görme Aracı (Unified Vision)

Eski `analyze_view`, `read_screen`, `find_ui_element` yerine tek ve güçlü bir araç.

### 3. `see_screen`
Ekranı görmek, metin okumak (OCR) veya element bulmak için.

- **Kullanım:** `see_screen(task, region, find)`
- **Parametreler:**
  - `task`: `None` (analiz), `'oku'` (OCR)
  - `region`: `'tam'`, `'sol'`, `'sağ'`, `'üst'`, `'alt'`, `'merkez'` (Odaklanma)
  - `find`: Aranacak element adı (Buton koordinatlarını verir)

**Örnekler:**
```python
# Genel ekran analizi
see_screen()

# Sadece sağ tarafı oku
see_screen(task="oku", region="sağ")

# Buton bul
see_screen(find="Spotify play button")
# Çıktı: "Found 'Spotify play button' at [950, 540]..."
```

---

## 💻 Sistem ve Sanal Workspace

### 4. `virtual_input`
Sanal çalışma alanına veya ana ekrana girdi göndermek için. Eski `click_in_workspace`, `type_in_workspace` yerine.

- **Kullanım:** `virtual_input(action, x, y, text, window)`
- **Eylemler:** `'click'`, `'type'`, `'key'`, `'focus'`

**Örnekler:**
```python
# Tıklama
virtual_input(action="click", x=500, y=300)

# Yazma
virtual_input(action="type", text="Merhaba Dünya")
```

### 5. `verify_code_quality`
Kod kalitesini kontrol etmek için.

- **Kullanım:** `verify_code_quality(filepath, actions)`
- **Actions:** `['lint', 'format', 'test']`

**Örnek:**
```python
verify_code_quality(filepath="main.py", actions=["lint", "format"])
```

---

## 🛠️ Diğer Önemli Araçlar

| Araç                   | Açıklama                                                |
| ---------------------- | ------------------------------------------------------- |
| `computer_control`     | Ana masaüstü fare/klavye kontrolü (Dikkatli kullanın)   |
| `run_terminal_command` | Terminal komutu çalıştırma (Tehlikeli komutlar engelli) |
| `web_search`           | İnternet araması (Tavily/DDG fallback mekanizmalı)      |
| `visit_webpage`        | Web sayfası içeriğini okuma                             |
| `write_code_advanced`  | Calcoder Pro ile gelişmiş kod üretimi                   |
| `youtube_analyzer`     | YouTube videolarını analiz etme                         |

---

## ⚠️ Kullanımdan Kaldırılanlar (Deprecated)

Aşağıdaki araçlar artık kullanılmamalıdır (çağrılırsa uyarı verirler):

- **Hafıza:** `save_context`, `remember_this`, `log_mood`, `learn_from_feedback`, `search_chat_history`, `recall_memory`
- **Görme:** `find_ui_element`, `analyze_view`, `read_my_emotion`
- **Kod:** `run_linter`, `format_code`, `run_tests`
- **Sistem:** `run_neofetch`
- **Input:** `click_in_workspace`, `type_in_workspace` (yerine `virtual_input`)

---
*Generated by Atomik Optimization Protocol v2.0*
